name: Sovereignty Check

on:
  pull_request:
  push:
    branches: [main]

jobs:
  verify-sovereignty:
    runs-on: ubuntu-latest
    name: Verify Docker-only deployment

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Check for cloud service violations
        run: |
          set -euo pipefail

          echo "🔍 Scanning for sovereignty violations..."
          echo ""

          # Check 1: Block references in workflows
          echo "Checking workflows..."
          if rg -n -i "vercel|supabase|VERCEL_|SUPABASE_" .github/workflows; then
            echo ""
            echo "❌ SOVEREIGNTY VIOLATION DETECTED"
            echo "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"
            echo "Vercel or Supabase references found in workflows."
            echo "MAIA-PAI uses Docker-only deployment for sovereignty."
            echo ""
            echo "Please remove all cloud service integrations."
            exit 1
          fi

          # Check 2: Block common repo artifacts
          echo "Checking for forbidden artifacts..."
          bad_paths=(
            ".vercel"
            "vercel.json"
            ".vercelignore"
            "_vercelignore"
            ".vercel-build-trigger"
            "supabase"
          )

          violation_found=false
          for p in "${bad_paths[@]}"; do
            if [[ -e "$p" ]]; then
              echo "❌ Found forbidden path: $p"
              violation_found=true
            fi
          done

          if [[ "$violation_found" == "true" ]]; then
            echo ""
            echo "❌ SOVEREIGNTY VIOLATION DETECTED"
            echo "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"
            echo "Vercel or Supabase artifacts found in repository."
            echo "MAIA-PAI uses Docker-only deployment for sovereignty."
            echo ""
            echo "Please remove all cloud service artifacts."
            exit 1
          fi

          echo "✅ Sovereignty check passed"
          echo "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"
          echo "✓ No workflow violations detected"
          echo "✓ No forbidden artifacts found"
          echo "✓ Docker-only deployment verified"
