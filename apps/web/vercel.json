{
  "framework": "nextjs",
  "buildCommand": "rm -rf node_modules && npm cache clean --force && npm install --no-package-lock && npm run build",
  "installCommand": "rm -rf node_modules .next && npm cache clean --force && npm install --no-package-lock",
  "functions": {
    "app/**/*.ts": {
      "maxDuration": 60,
      "runtime": "nodejs18.x"
    },
    "app/api/**/*.ts": {
      "maxDuration": 60,
      "runtime": "nodejs18.x"
    }
  }
}