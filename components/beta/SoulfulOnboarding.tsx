'use client';

/**
 * SOULFUL ONBOARDING - ENHANCED WITH SACRED FLOW
 * Legacy wrapper that redirects to the new Sacred Onboarding Flow
 *
 * This component now serves as a bridge between the old onboarding system
 * and the new consciousness-aware Sacred Onboarding Flow that integrates
 * with the archetypal recognition and MAIA consciousness system.
 */

import { SacredOnboardingFlow } from '@/components/maia/SacredOnboardingFlow';

export function SoulfulOnboarding({ initialName }: { initialName: string }) {
  // Enhanced with Sacred Onboarding Flow integration
  // Redirects to the new consciousness-aware onboarding experience
  return <SacredOnboardingFlow initialName={initialName} />;
}